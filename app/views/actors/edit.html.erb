<div class="page-header">
  <h1>Edit: <%= @actor.name %></h1>
</div>
<h3>Has been in:</h3>
<ul>
  <% @actor_movies.each do |movie| %>
    <li><%= movie.title %></li>
  <% end %>
</ul>
<hr>
  

<h1><%= @actor.name %></h1>



  <%= form_tag add_movie_path do |f|%>
    <select name="movie_id">
      <% @moviesremain.each do |flick| %>
      <option value="<%=flick.id%>"><%=flick.title %></option>
      <% end %>
    </select>
    <input type="submit" value="Add">
  <% end %>

<!--
< %= form_for @actor do |f| %>
  < %= f.text_field :name %>
  <ul class="noDot">
    < % @movies.each do |movie| %>
      <li>< %= check_box_tag movie.id, movie.title %> < %= movie.title %> </li>
    < % end %>
  </ul>

  < %= f.submit "Update" %>
< % end %>
-->

<!--
< %= simple_form_for(@actor, html: { class: 'form-horizontal' }) do |f| %>
  < %= f.text_field :name %>
  <ul class="noDot">
    < % @movies.each do |movie| %>
      <li>< %= check_box_tag movie.id, movie.title %> < %= movie.title %> </li>
    < % end %>
  </ul>
  < %= f.submit "Update" %>
< % end %>
 -->




<!--
"@actor_movies", movie.title, @actor.movie.title.include?(movie.title) %>
-->