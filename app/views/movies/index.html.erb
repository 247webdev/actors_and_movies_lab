<div class="page-header">
  <h1>Movies</h1>
</div>

<ul>
  <% @movies.each do |film| %>
  <li><%= link_to "#{film.title} (#{film.year})", film %></li>
  <% end %>
</ul>

<%= link_to "Add movie", new_movie_path, :class=>"btn btn-warning btn-sm" %>
